# Используем официальный образ OpenJDK для Java 11
FROM openjdk:11

# Установка рабочей директории внутри контейнера
WORKDIR /app

# Копирование JAR файла с проектом и распаковка его в контейнере
COPY ToDoMagic.jar /app/

# Указываем команду для запуска приложения при старте контейнера
CMD ["java", "-jar", "ToDoMagic.jar"]

# Открываем порт 8081, который используется вашим приложением
EXPOSE 8081





